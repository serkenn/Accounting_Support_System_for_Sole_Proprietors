# サードパーティの権利表示

## 同梱（vendoring）したコード

現時点ではありません。

同梱するたびに、以下を必ず記録します。

- 取得元のリポジトリ URL
- **取り込んだコミットハッシュ**
- ライセンス種別とライセンス全文
- 改変の有無（改変した場合は `MODIFICATIONS.md` へ）

予定されているもの:

| 対象 | 用途 | ライセンス | 状態 |
|------|------|-----------|------|
| デジタル庁デザインシステム（React コードスニペット） | Web UI の Layer 1 | MIT | 未取り込み |

## 実行時に呼び出す外部プログラム

**本パッケージは以下を Python の `import` では利用せず、サブプロセスとして
コマンドラインから呼び出します。**

| プログラム | ライセンス | 呼び出し方 |
|-----------|-----------|-----------|
| `bean-check`（Beancount） | GPL-2.0-only | `subprocess` |
| `bean-query`（beanquery） | GPL-2.0 | `subprocess`（結果は CSV で受け取る） |

### なぜサブプロセスに限定するか

Beancount および beanquery は **GPL-2.0-only** です。
本パッケージは MIT で配布するため、これらを `import` して結合すると
ライセンスの整合が取れなくなります。

独立したプログラムをコマンドラインから起動し、標準入出力とファイルだけで
やり取りする形であれば、派生物とはみなされにくいと考えています。
この境界を守るため、`import beancount` / `import beanquery` を
**CI で機械的に禁止**しています（`tests/test_license_boundary.py`）。

> この整理は法的助言ではありません。配布方法を変える場合は、
> 改めて各ライセンスの条項を確認してください。

## 依存ライブラリ

依存を追加するたびにライセンスを確認し、非互換が混ざっていないかを
CI で検査します。
